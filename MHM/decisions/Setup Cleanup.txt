political_decisions = {

	global_setup = {
		picture = gtfo
		potential = {
			NOT = { has_global_flag = daily_cleanup_on }
			ai = yes
		}
		
		allow = {
		}
		
		effect = {
			set_global_flag = daily_cleanup_on
			CLN = { country_event = 47777787 }
		}
	}
}